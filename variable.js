// 1. declare let a = 5;
let a = 5;
console.log(a);

// 2.declare const PI = 3.14;
const PI = 3.14;
console.log("the value of PI is", PI);

// 3.declare a variable name with value "Alice";
var name = "Alice";
console.log(name);

// 4.assign a number to a variable and reassign a strng;
let value = 10;      
//   let allows you to reassign values of different types (number → string)
 value = "Hello";
console.log(value);

// 5.   assign a boolean to a variable and reassign a number ;
let flag = "true";
flag = 1234;
console.log(flag);

// 6.declare let x = 10 , then x = 20;
let x = 10;
 x = x *2;
 console.log(x);

// 7. declare three variables q =1, w=2, e=3.
let q = 1, w = 2, e = 3;
console.log(q,w,e); 

// 8.assign the sum of two variables to a third.
let sum =  2 + 3;
answer = sum;
console.log(answer);

// 9.assign a variable's value to another;
let english = 100;
java = english;
console.log(java);

// 10.declare multiple variables in one line.
let p = 4 , l = 5, m = 6;
console.log(p, l, m);   //o/p is 4 5 6

// 11.declare ar = 1, br = ar + 1, cr = br + ar.
let ar = 1, br = ar + 1, cr = ar + br ;
console.log(ar , br, cr);   //o|p is  1 2 3

// 12.declare value = 10 ,double = value * 2.
let valuee = 50;
double = valuee * 2;
console.log(double);  //o/p is 100
// 13.assign the product of three variables to a fourth.
let xr = 1, yr = 2, zr = 3;
let result = xr * yr * zr;
console.log(result);          //o/p is 6

// 14.use typeof on a variable
let d = 20 ;
console.log(typeof(d));      // o/p is number

// 15.reassign a let variable with a different type.
let data = true;    //boolean
  data = 242;       //number
  data = {name: "mangesh" };  //object
  console.log(data);           // o/p is name : "mangesh"

//   16.assign the result of an expression to a variable x = 5 *  2 + 3.
     let exp = 5 * 2 + 3;
     result = exp;
     console.log(result);

//  17.Use backticks to assign a template string: msg = `Hi $ {name}`
     let nam = "Mangesh";
     let msg = `Hi ${nam}`;
     console.log(msg);
     
// 18.assign a string using concatenation: greeting = "Hi" +  name .
let str1 = "Hi";
let str2 = "mangesh";
 let expr = str1.concat (str2);   //or str1 + str2 ;  we can use plus for concat
 console.log(expr);

//  19.declare let empty = null.
  let empty = null;
  console.log(empty);  

//   20. assign undefined to a variable.
let und ;
console.log(und);    //output willbe undefined if value is not given then o/p will be undefined

// 21.create an object : let user = {name : "Alice",  age: 25}
let user = {
    nav: "Alice",
    age : 25
};
console.log(user);

// 22.create a nested object: let profile = { user: { name : "Alice"}}
let profile = {
     user : { 
        namee : "Alice"
    }
};
console.log(profile);

// 23.destructure an object to get a property;

//24.  reassign a destructured variable

// 25. create an array : let nums = [1, 2, 3].
let nums = [1, 2, 3];
console.log(nums);




  
 

    
     
     

  




 


 


     








